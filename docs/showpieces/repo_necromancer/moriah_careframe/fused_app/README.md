# MoriahCareFrame

This is a Repo Necromancer fused-app packet generated by CAM_Codx/CAM_CAM.

MoriahCareFrame turns workspace inventory plus code-grafting logic into a local subsystem transplant desk: find reusable modules, preflight the target, and produce a patch plan before copying code.

## Run

```bash
python repo_necromancer_demo.py --evidence ../evidence.json
open index.html
```

## Source Repos

- `moriah_omega`: No README summary found. Source path: `/Volumes/WS4TB/WS4TBr/MORIAH/moriah_omega`. Git repo: `False`. Git head: `not available`. Dirty: `False`.
- `Proto_Dev_Req`: No README summary found. Source path: `/Volumes/WS4TB/careframe/Proto_Dev_Req`. Git repo: `True`. Git head: `194fc54`. Dirty: `True`.

## What was revived

- **Workspace inventory and repo signal profiler** from Source A: A read-only scanner that turns source trees into evidence JSON before any graft.
- **Care-oriented preflight and rollback framing** from Source B: A target-safety review that names warnings, required checks, and rollback steps.
- **Transplant desk glue** from New glue: A merged CLI/report packet that explains what to borrow before copying code.

## Safe merge plan

- **1. Freeze source receipts**: write docs/source_receipts.md, evidence/source_profiles.json; verify with tests/test_source_receipts.py; rollback: Delete generated receipts; no source repo rollback needed because inputs are read-only.
- **2. Build transplant planner**: write moriah_careframe/planner.py, moriah_careframe/cli.py; verify with tests/test_planner.py, tests/test_cli_demo.py; rollback: Revert planner and CLI files; keep receipts for audit continuity.
- **3. Add reviewable patch output**: write patch_plan.md, patches/README.md; verify with tests/test_patch_plan_contract.py; rollback: Discard generated patch plan and rerun from unchanged receipts.

## Verification

The packet-level verification record lives in `../TEST_RESULTS.md`.
