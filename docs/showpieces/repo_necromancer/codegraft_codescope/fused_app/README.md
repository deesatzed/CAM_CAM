# CodeGraftScope

This is a Repo Necromancer fused-app packet generated by CAM_Codx/CAM_CAM.

CodeGraftScope turns workspace inventory plus code-grafting logic into a local subsystem transplant desk: find reusable modules, preflight the target, and produce a patch plan before copying code.

## Run

```bash
python repo_necromancer_demo.py --evidence ../evidence.json
open index.html
```

## Source Repos

- `codegraft`: Improve your Python codebase by grafting valuable code from external sources.
- `codescope`: No README summary found.
